# 模型 API 凭据（按需填写；默认使用 Claude）
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=

# 可选：自定义 OpenAI 兼容端点（通用兜底 Provider）
OPENAI_COMPAT_API_KEY=
OPENAI_COMPAT_BASE_URL=

# 默认 Provider 与模型（不填则用代码内置默认：claude）
DEFAULT_PROVIDER=claude
# DEFAULT_MODEL=claude-opus-4-8

# 会话数据目录（默认 ../data）
# DATA_DIR=
